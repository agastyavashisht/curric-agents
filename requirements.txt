# Core orchestration
langgraph>=0.2.0
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-anthropic>=0.2.0
langchain-google-genai>=2.0.0
langchain-groq>=0.1.0

# Config
python-dotenv>=1.0.0

# HTTP (used by online_db.py for Supabase REST API — no C extensions)
requests>=2.31.0

# Data & eval (pure Python / pure numpy — no torch, no scipy, no sklearn)
pandas>=2.0.0
numpy>=1.26.0

# Web UI
streamlit>=1.35.0
altair>=5.0.0
